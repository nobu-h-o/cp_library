# CP Library/Template

This repository is for storing my template and library when sving problems in c++ (albeit which isn't often).
This library uses the ACL (AtCoder Library), which is available [here](https://github.com/atcoder/ac-library)